# Story 1.3: Component Children Slotting

## Status

Ready

**Complexity:** L (Large) — design spike obrigatório, mecanismo de tree-building via stack, nested slotting

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["zig build test", "zig build check", "zig build run"]
```

## Story

**As a** Zig UI developer,
**I want** to pass child elements into custom components via a slotting mechanism,
**so that** I can build reusable container components like cards, modals, and layouts that accept arbitrary content.

## Acceptance Criteria

1. `forbear.componentChildrenSlot()` marks the insertion point for children within a component's render function
2. A component can receive a block of child nodes via the composition API
3. Children are rendered at the correct position in the component's layout tree
4. Nested slotting works (component with slot inside component with slot)
5. A `Card` component in `playground.zig` demonstrates the slotting API with custom content passed in
6. `zig build test` and `zig build check` pass without errors

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Scope

**IN Scope:**
- Um único slot padrão por componente (`componentChildrenSlot()`)
- Mecanismo de passagem de children via chamada de `component()`
- Nested slotting (slot dentro de slot)
- Demo `Card` em `playground.zig`

**OUT of Scope:**
- Named slots (ex: `slot="header"`)
- Multi-slot (mais de um slot por componente)
- Slot com conteúdo padrão (fallback content)
- Slots condicionais / scoped slots

## Dependencies

- **BLOCKER: Spike obrigatório** — decisão de design deve ser documentada em `.ai/decision-logs/` antes da implementação
- Revisar `element()` e `component()` em `src/root.zig` integralmente antes de qualquer código
- `frameNodeParentStack` e `frameNodePath` devem ser compreendidos (estrutura de tree-building)

## Tasks / Subtasks

- [ ] Design the slotting API (spike — resolve approach before implementing) (AC: 1, 2)
  - [ ] Review `Component` struct in `src/node.zig` (line ~473): `function: *const fn(props: ?*anyopaque) anyerror!Node`
  - [ ] Review `frameNodeParentStack` and `frameNodePath` in Context (`src/root.zig`) — understand tree building
  - [ ] Decide: children passed as props field OR via context slot mechanism
  - [ ] Document decision in `.ai/decision-logs/` before implementing
- [ ] Implement `componentChildrenSlot()` function in `src/root.zig` (AC: 1, 3)
  - [ ] Marks current node position as slot target
  - [ ] During component resolution, inserts pending children at slot position
  - [ ] If no children provided, slot renders nothing (empty)
- [ ] Implement children-passing mechanism in `component()` call site (AC: 2)
  - [ ] Extend `Component` struct or component() call to accept optional children nodes
  - [ ] Children nodes are captured before component function executes
  - [ ] Children are available to `componentChildrenSlot()` during component render
- [ ] Validate nested slotting (AC: 4)
  - [ ] Component A with slot contains Component B with slot
  - [ ] Each slot independently resolves its own children
- [ ] Add `Card` component example to `playground.zig` (AC: 5)
  - [ ] `Card` renders a styled container with border/padding
  - [ ] Caller passes arbitrary content (text, nested elements) via slot
  - [ ] Example demonstrates: `Card({ someButton, someText })`
- [ ] Write unit tests (AC: 6)
  - [ ] Test: component with slot renders children at correct position
  - [ ] Test: component with slot and no children renders slot as empty
  - [ ] Test: nested slots resolve independently
  - [ ] Run `zig build test`

## Dev Notes

**Current Component API (from `src/node.zig` and `src/root.zig`):**

```zig
// Current Component definition (src/node.zig ~line 473)
pub const Component = struct {
    function: *const fn (props: ?*anyopaque) anyerror!Node,
    id: usize,
    props: *anyopaque,
};

// Current usage pattern
try component(arenaAllocator, MyComponent, MyProps{...});
```

**Context state relevant to tree building (`src/root.zig`):**
- `frameNodeParentStack: std.ArrayList(*Node)` — current parent stack
- `frameNodePath: std.ArrayList(usize)` — position path in tree
- `previousPushedNode: ?*const Node` — last pushed node
- `rootNodes: std.ArrayList(Node)` — root nodes for current frame

**Design Consideration:**
The slotting mechanism needs to capture children before the component function runs, then inject them at the `componentChildrenSlot()` call site. One approach:
- Add a `pendingSlotChildren: ?std.ArrayList(Node)` field to Context
- Before calling component function, stash children in context
- `componentChildrenSlot()` reads from context and inserts children into current parent

**Key Files:**
| File | Change Needed |
|---|---|
| `src/node.zig` | Possibly extend `Component` struct to carry children |
| `src/root.zig` | Add `componentChildrenSlot()`, extend `component()` to accept children |
| `playground.zig` | Add `Card` component demo |

**IMPORTANT — Spike Required:**
This story requires a design spike before implementation. The tree-building mechanism uses a stack-based approach that must be understood before adding slot injection. Review `element()` and `component()` functions fully in `src/root.zig` before writing any code.

**Source:** `docs/prd.md` — Story 1.3 | `docs/architecture.md` — Epic 1 context

### Testing

- Tests inline in `src/root.zig` using `test "Slotting - {scenario}"` blocks
- Visual verification via `zig build run` (playground `Card` demo)
- Run: `zig build test` for unit tests
- Run: `zig build check` to verify all examples compile

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-02-21 | 1.0 | Story created from Epic 1 PRD | River (sm) |
| 2026-02-22 | 1.1 | @po validation: GO Condicional (7/10) — Draft → Ready, adicionado complexity/scope/dependencies, spike obrigatório documentado | Pax (po) |

## Dev Agent Record

### Agent Model Used

_(to be filled by @dev)_

### Debug Log References

_(to be filled by @dev)_

### Completion Notes List

_(to be filled by @dev)_

### File List

_(to be filled by @dev)_

## QA Results

_(to be filled by @qa)_
