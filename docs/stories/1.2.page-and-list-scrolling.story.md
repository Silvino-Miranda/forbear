# Story 1.2: Page and List Scrolling

## Status

Ready

**Complexity:** XL (Extra Large) — 8+ arquivos, Vulkan scissor stack, platform layer (Linux/macOS/Windows), spring physics integration

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["zig build test", "zig build check", "zig build run"]
```

## Story

**As a** Zig UI developer,
**I want** to make any container element scrollable when its content overflows,
**so that** I can build pages with long content and scrollable lists without content being clipped or invisible.

## Acceptance Criteria

1. An element can be marked as scrollable via an `overflow` style property (e.g., `overflow: .scroll`)
2. Vertical scroll works with mouse wheel input on Linux (Wayland), macOS, and Windows
3. Scroll animation uses the existing spring physics (`useSpringTransition`) for smooth deceleration
4. Content that overflows the container bounds is not rendered (correct Vulkan scissor clipping)
5. A working scroll demo exists in `playground.zig` showing content taller than 2× the window height
6. `zig build test` and `zig build run` (playground) pass without errors

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Scope

**IN Scope:**
- `overflow: .scroll` e `overflow: .hidden` no style system
- Scroll vertical com mouse wheel (Linux/Wayland, macOS, Windows)
- Animação via `useSpringTransition` existente
- Vulkan scissor clipping para contenção de conteúdo
- Demo em `playground.zig` com lista 2× taller que a janela

**OUT of Scope:**
- Scroll horizontal (defer — não necessário para MVP)
- Touch/trackpad gestures (defer)
- Scroll snap / scroll anchoring
- APIs programáticas (`scrollTo`, `scrollIntoView`)
- Scrollbar visual customizável (nenhuma scrollbar — MVP invisível)

## Dependencies

- Nenhuma story pré-requisito obrigatória
- `useSpringTransition()` já implementado em `src/root.zig` ~line 451 — **deve estar funcional**
- Platform layer (linux.zig, macos.zig, windows.zig) já deve ter estrutura de event handling

## Tasks / Subtasks

- [ ] Add `overflow` property to style system (AC: 1)
  - [ ] Add `pub const Overflow = enum { visible, scroll, hidden };` to `src/node.zig`
  - [ ] Add `overflow: Overflow = .visible` to `Style` struct in `src/node.zig`
  - [ ] Add `overflow: ?Overflow = null` to `IncompleteStyle` struct in `src/node.zig`
  - [ ] Handle `overflow` in `IncompleteStyle.completeWith()` (default: `.visible`)
- [ ] Add scroll offset state to `LayoutBox` (AC: 3, 4)
  - [ ] Add `scrollOffset: Vec2 = .{0, 0}` to `LayoutBox` in `src/layouting.zig`
  - [ ] `clipRect: ?Rect = null` — add `Rect` type if not present: `pub const Rect = struct { pos: Vec2, size: Vec2 };`
  - [ ] When `style.overflow == .scroll`, set `layoutBox.clipRect = .{ .pos = layoutBox.position, .size = layoutBox.size }`
- [ ] Implement `useScroll()` hook in `src/root.zig` (AC: 2, 3)
  - [ ] Signature: `pub fn useScroll(config: ScrollConfig) !f32` where `ScrollConfig = struct { direction: enum { vertical, horizontal } }`
  - [ ] Internally calls `useSpringTransition(scrollTarget, springConfig)` — reuses existing spring physics
  - [ ] `scrollTarget` is updated by mouse wheel events
  - [ ] Returns current eased scroll offset value
- [ ] Mouse wheel event routing (AC: 2)
  - [ ] Add `.scroll` event variant to `Event` union in `src/root.zig`: `scroll: Vec2`
  - [ ] Platform layer (linux.zig, macos.zig, windows.zig): translate native scroll events to forbear `.scroll` event
  - [ ] Hit-test: route scroll event to innermost hovered element with `overflow == .scroll`
- [ ] Vulkan scissor clipping in renderer (AC: 4)
  - [ ] In `src/graphics.zig`: add scissor stack: `scissorStack: [8]Rect` and `scissorDepth: u8`
  - [ ] Before rendering a `LayoutBox` with `clipRect != null`: call `vkCmdSetScissor` with the clipRect
  - [ ] After rendering, restore previous scissor state from stack
  - [ ] Children positions are offset by `scrollOffset` before rendering (translate by -scrollOffset)
- [ ] Add scroll demo to `playground.zig` (AC: 5)
  - [ ] Create a scrollable container with >2× window height content (e.g., 20 text items)
  - [ ] Verify smooth scrolling with spring physics
- [ ] Write unit tests (AC: 6)
  - [ ] Test: element with `overflow: .scroll` gets clipRect set in layout
  - [ ] Test: `useScroll()` state persists across frames
  - [ ] Run `zig build test`

## Dev Notes

**Architecture Decision (from `docs/architecture.md`):**

This story uses the **4-layer scroll architecture**:

```
Layer 1: Style    → overflow: Overflow enum in node.zig
Layer 2: Layout   → clipRect: ?Rect in LayoutBox (layouting.zig)
Layer 3: Render   → vkCmdSetScissor scissor stack in graphics.zig
Layer 4: Input    → useScroll() hook + mouse wheel routing in root.zig
```

**Reusing existing infrastructure:**
- `useSpringTransition(target, config)` already implemented in `src/root.zig` (line ~451)
- `SpringConfig = struct { stiffness, damping, mass }` already defined
- `hoveredElementKeys: std.ArrayList(u64)` in Context — use for scroll hit-testing
- Scroll event routing: find outermost (or innermost?) scrollable element in `hoveredElementKeys`
- **Decision from architecture doc:** innermost element in hover stack receives scroll event

**Vulkan Scissor Notes:**
- `vkCmdSetScissor` requires integer pixel coordinates (VkRect2D with offset + extent)
- Convert `Rect` (f32) to `VkRect2D` (i32) at render time
- Initial scissor state = full viewport (no clipping) when scissorDepth == 0
- CRITICAL: Scissor state must be restored after each scrollable container renders

**Key Files:**
| File | Change Needed |
|---|---|
| `src/node.zig` | Add `Overflow` enum, `overflow` field to `Style`/`IncompleteStyle` |
| `src/layouting.zig` | Add `clipRect` + `scrollOffset` to `LayoutBox`, set when overflow=scroll |
| `src/graphics.zig` | Add scissor stack, apply `vkCmdSetScissor` per clipRect |
| `src/root.zig` | Add `useScroll()` hook, `.scroll` event type, wheel routing |
| `src/window/linux.zig` | Translate Wayland scroll events |
| `src/window/macos.zig` | Translate NSScrollWheel events |
| `src/window/windows.zig` | Translate WM_MOUSEWHEEL events |
| `playground.zig` | Add scrollable list demo |

**Recommended Spring Config for scrolling:**
```zig
// Feels natural for scroll — high stiffness, moderate damping
SpringConfig{ .stiffness = 300.0, .damping = 30.0, .mass = 1.0 }
```

**Source:** `docs/architecture.md` — Section 2 (Scrolling Architecture)

### Testing

- Tests inline in `src/root.zig` using `test "Scrolling - {scenario}"` blocks
- Visual verification via `zig build run` (playground demo)
- Run: `zig build test` for unit tests
- Run: `zig build check` to verify all examples compile

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-02-21 | 1.0 | Story created from Epic 1 PRD + architecture doc | River (sm) |
| 2026-02-22 | 1.1 | @po validation: GO Condicional (7/10) — Draft → Ready, adicionado complexity/scope/dependencies | Pax (po) |

## Dev Agent Record

### Agent Model Used

_(to be filled by @dev)_

### Debug Log References

_(to be filled by @dev)_

### Completion Notes List

_(to be filled by @dev)_

### File List

_(to be filled by @dev)_

## QA Results

_(to be filled by @qa)_
